name: Docs
on: [push, pull_request, workflow_dispatch]
jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: checkout-oda-ca-docs
        uses: actions/checkout@v2
        with:
          path: oda-ca-docs
      - name: checkout-oda-ca
        uses: actions/checkout@v2
        with:
          repository: tmforum-oda/oda-ca
          path: oda-ca
      - name: checkout-oda-canvas-charts
        uses: actions/checkout@v2
        with:
          repository: tmforum-oda/oda-canvas-charts
          path: oda-canvas-charts
      - name: checkout-oda-component-ctk
        uses: actions/checkout@v2
        with:
           repository: tmforum-oda/oda-component-ctk
           path: oda-component-ctk
      - uses: actions/setup-python@v2
      - name: debug-1
        run: ls
      - name: debug-2
        run: pwd
      - name: debug-3
        run: ls
        working-directory: oda-ca-docs/doc
      - name: debug-4
        run: pwd
        working-directory: oda-ca-docs/doc
      - name: Install dependencies
        working-directory: oda-ca-docs/doc
        run: |
          pip install sphinx sphinx_rtd_theme
      - name: Sphinx build
        working-directory: oda-ca-docs/doc
        run: |
          sphinx-build . _build
      - name: Sphinx build debug 1
        working-directory: oda-ca-docs/doc
        run: |
          pwd
      - name: Sphinx build debug 2
        working-directory: oda-ca-docs/doc
        run: |
          ls ../../oda-ca
      - name: Sphinx build debug 3
        working-directory: oda-ca-docs/doc
        run: |
          ls ../../oda-ca/controllers
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' }}
        with:
          publish_branch: gh-pages
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: oda-ca-docs/_build/
          force_orphan: true